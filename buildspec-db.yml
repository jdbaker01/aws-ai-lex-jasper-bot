version: 0.2

phases:
  install:
    commands:
      - echo "Nothing to install."
  build:
    commands:
      - echo Build started on `date`
      - sh build-db.sh
  post_build:
    commands:
      - echo Build completed on `date`
artifacts:
  files:
    - test.py
