version: 0.2

phases:
  install:
    commands:
      - echo "Nothing to install."
  build:
    commands:
      - echo Build started on `date`
      - bash -v build-db.sh
  post_build:
    commands:
      - echo Build completed on `date`
